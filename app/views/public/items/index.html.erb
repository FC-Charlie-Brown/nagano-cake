<h3>商品一覧</h3>
<%= "全#{@items.count}件" %>
<% @items.each do |item|%>
<div>
  <%= link_to attachment_image_tag(item, :image, :fill, 200, 200, format: 'jpeg', fallback: "no_image.jpg", size: "200x200"), item_path(item.id) %>
  <p><%= item.name %></p>
  <p><%= item.price %></p>
</div>
<% end %>

