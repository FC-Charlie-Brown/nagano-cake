<%=  attachment_image_tag @item, :image, :fill, 300, 300, format: 'jpeg', fallback: "no_image.jpg", size: "300x300" %>

  <h3><%= @item.name%></h3>
  <%= @item.introduction%>
  <h4><%= @item.price %></h4>
  <a>(税込)</a>