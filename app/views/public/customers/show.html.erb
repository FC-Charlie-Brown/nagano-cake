<h5 class= "toptext2 text-center"><%= "#{current_customer.last_name}" "#{current_customer.first_name}" %>さんのマイページ</h5>

<div class="row form-group">
  <legend class="col-form-label col-sm-2">登録情報</legend>
  <%= link_to '編集する', edit_customers_path,{class:'btn btn-success'} %>
</div>

<div class="row">
  <table class="table table-bordered col-sm-7">
    <tr>
      <th style="width:25%" class="bg-light"> 氏名</th>
      <td class=""><%= current_customer.last_name %>   <%= current_customer.first_name %></td>
    </tr>
    <tr>
      <th class="bg-light">カナ</th>
      <td><%= current_customer.last_name_kana %>   <%= current_customer.first_name_kana %></td>
    </tr>
    <tr>
      <th class="bg-light">郵便番号</th>
      <td><%= current_customer.postal_code %></td>
    </tr>
    <tr>
      <th class="bg-light">住所</th>
      <td><%= current_customer.address %></td>
    </tr>
    <tr>
      <th class="bg-light">電話番号</th>
      <td><%= current_customer.telephone_number %></td>
    </tr>
    <tr>
      <th class="bg-light">メールアドレス</th>
      <td><%= current_customer.email %></td>
    </tr>
  </table>
</div>

<div class="row form-group">
  <legend class="col-form-label col-sm-2">配送先</legend>
  <%= link_to '一覧を見る', addresses_path,{class:'btn btn-primary col-sm-2'} %>
</div>

<div class="row form-group">
  <legend class="col-form-label col-sm-2">注文履歴</legend>
  <%= link_to '一覧を見る', orders_path,{class:'btn btn-primary col-sm-2'} %>
</div>